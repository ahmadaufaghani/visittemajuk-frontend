export interface Banner {
    id: number,
    title: string,
    description: string,
    menu: string,
    image: string
}

export interface BannerPayload {
    title: string,
    description: string,
    menu: string,
    image: string
}